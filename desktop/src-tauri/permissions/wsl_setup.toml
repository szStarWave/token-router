[[permission]]
identifier = "allow-wsl-detect-environment"
description = "Detects running WSL distros and agent setup state."
commands.allow = ["wsl_detect_environment"]

[[permission]]
identifier = "allow-wsl-configure-agent"
description = "Writes Token Router provider config into a single WSL agent config."
commands.allow = ["wsl_configure_agent"]
